# fuseki-docker
Documentation follows.
For a first test build and run it (docker run (-d) -p ????:3030 <docker_image_name>). Username and password for the web interface (http://localhost:????) is 'admin'.
